<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="oauth_provider.list">
      <field name="name">oauth_provider list</field>
      <field name="model">oauth_provider.oauth_provider</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="oauth_provider.action_window">
      <field name="name">oauth_provider window</field>
      <field name="res_model">oauth_provider.oauth_provider</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="oauth_provider.action_server">
      <field name="name">oauth_provider server</field>
      <field name="model_id" ref="model_oauth_provider_oauth_provider"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="oauth_provider" id="oauth_provider.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="oauth_provider.menu_1" parent="oauth_provider.menu_root"/>
    <menuitem name="Menu 2" id="oauth_provider.menu_2" parent="oauth_provider.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="oauth_provider.menu_1_list" parent="oauth_provider.menu_1"
              action="oauth_provider.action_window"/>
    <menuitem name="Server to list" id="oauth_provider" parent="oauth_provider.menu_2"
              action="oauth_provider.action_server"/>
    -->
  </data>
</odoo>